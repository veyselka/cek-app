<Window x:Class="CheckPrintApp.UI.Views.SaveProfileDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Profil Kaydet"
        Width="400" Height="210"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="#F5F5F5"
        WindowStyle="ToolWindow">

    <Window.Resources>
        <Style TargetType="Button">
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="6"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Başlık -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,12">
            <TextBlock Text="🏦" FontSize="22" Margin="0,0,8,0"/>
            <TextBlock Text="Banka Profili Kaydet"
                       FontSize="16" FontWeight="Bold"
                       Foreground="#1565C0"
                       VerticalAlignment="Center"/>
        </StackPanel>

        <!-- Açıklama -->
        <TextBlock Grid.Row="1"
                   Text="Mevcut kalibrasyon ayarları bu isimle kaydedilecek."
                   FontSize="11" Foreground="#616161"
                   Margin="0,0,0,10"
                   TextWrapping="Wrap"/>

        <!-- İsim girişi -->
        <TextBox x:Name="ProfileNameTextBox"
                 Grid.Row="2"
                 Height="36"
                 Padding="10,6"
                 FontSize="14"
                 BorderBrush="#2196F3"
                 BorderThickness="2"
                 VerticalContentAlignment="Center"
                 MaxLength="50"
                 KeyDown="ProfileNameTextBox_KeyDown"/>

        <!-- Hata mesajı -->
        <TextBlock x:Name="ErrorText"
                   Grid.Row="3"
                   Foreground="#F44336"
                   FontSize="11"
                   Margin="0,4,0,0"
                   Visibility="Collapsed"/>

        <!-- Butonlar -->
        <Grid Grid.Row="4">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="10"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Button Grid.Column="0"
                    Content="İptal"
                    Background="#9E9E9E"
                    Foreground="White"
                    Click="CancelButton_Click"/>

            <Button Grid.Column="2"
                    Content="💾  Kaydet"
                    Background="#2196F3"
                    Foreground="White"
                    Click="SaveButton_Click"
                    IsDefault="True"/>
        </Grid>
    </Grid>
</Window>
